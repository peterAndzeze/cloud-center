server:
  port: 80
spring:
  thymeleaf:
    mode: HTML
    encoding: utf-8
    # 禁用缓存
    cache: false
    prefix: classpath:/META-INF/resources/templates
    suffix: .html
  resources:
    static-locations: classpath:/META-INF/resources/static